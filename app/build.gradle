apply plugin: 'android'
android {
    compileSdkVersion 8
    buildToolsVersion "19.1.0"

    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 8
    }

    buildTypes {
        release {
        }
    }
}

dependencies {
    compile 'com.android.support:gridlayout-v7:22.2.0'
}
